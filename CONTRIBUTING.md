# Contributing

Create a focused branch, keep personal data and secrets out of commits, preserve strict TypeScript and accessible behaviour, and add tests for logic changes. Run `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` before opening a pull request. Report security issues privately before public discussion.
